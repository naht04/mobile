name: mobile_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^1.2.0
  webview_flutter: ^4.13.1
  url_launcher: ^6.3.2
  http: ^1.6.0
  shared_preferences: ^2.5.5
  file_picker: ^11.0.2
  image_picker: ^1.2.0
  web_socket_channel: ^3.0.3
  pdfx: ^2.9.2
  flutter_webrtc: ^1.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Nếu Thản có dùng ảnh trong folder assets thì bỏ dấu # ở 2 dòng dưới này nhé:
  # assets:
  #   - assets/images/